package com.mgo.search.shell.render;

public interface Renderer {

    String LINE_SEPARATOR = System.lineSeparator();
    String HORIZONTAL_BAR = LINE_SEPARATOR + "--------------------------------------------------" + LINE_SEPARATOR;

}
