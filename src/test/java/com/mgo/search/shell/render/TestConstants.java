package com.mgo.search.shell.render;

class TestConstants {
    static final String LINE_SEPARATOR = System.lineSeparator();
    static final String HORIZONTAL_BAR = LINE_SEPARATOR + "--------------------------------------------------" + LINE_SEPARATOR;
}
